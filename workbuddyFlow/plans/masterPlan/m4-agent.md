# 模块计划 · Agent 真 ReAct（阶段4）

> 状态：规划中
> 关联架构：`architecture.md` 2.4 包表（`agent` 允许调 `tools` / `llm` / `graph`）、ADR-4、不变式 4
> 覆盖任务：v2-18 ~ v2-20

## 元信息
- 模块编号：M4
- 状态：规划中
- 创建：2026-09-11 + AI

## 目标
把"伪 ReAct（只有计划没有观察回环）"换成真 ReAct：每步 Thought→Action→Observation 必须回喂 LLM 再决策。

## 边界
- 允许新增：`agent/` 包（ReAct 循环骨架、步数/中断控制、事件流）
- 允许修改：`service` 编排（接入 agent 主循环）
- 禁止改动：校验（M1）、工具（M3 已独立）

## 分层边界
落在 `agent/`；编排 `tools` / `llm` / `graph`。不触碰 `session`（M5）与前端（M6）。原因：Agent 是"大脑"，编排既有能力，不改底层。

## 前置
- M3 工具体系可用（v2-15~17）
- M1 校验闭环可用

## 步骤
1. v2-18 Thought→Action→Observation 循环骨架：每步结果必须回喂 LLM 决策下一步
2. v2-19 最大步数 + 中断 + 全程事件流（思考 / 工具 / 校验实时推送）
3. v2-20 验收用例："画一个电商系统架构"自动走 检索→(工具)→生成→校验→修正

## 验收口径
1. 不变式 4：每步 Observation 必须回喂 LLM（单测可断言）
2. 步数达上限或中断时优雅停止并产出 partial 结果
3. v2-20 用例端到端跑通，事件流完整

## 覆盖测试数据
| 场景 | 输入 | 期望 |
|---|---|---|
| 正常 | "画一个电商系统架构" | 自动走 检索→工具→生成→校验→修正 出图 |
| 边界 | 故意让工具连续失败 | 步数上限触发，输出 partial + 记 tech-debt |

## 状态跟踪
- [ ] v2-18 ReAct 循环骨架
- [ ] v2-19 步数/中断/事件流
- [ ] v2-20 验收用例
