# activeLog.md — 执行流水（外置记忆，只追加）

> 谁在什么时候做了什么、证据是什么。每条必有证据（命令 + 结果），否则等于没记。
> 两种读法：① 会话恢复 → 只读顶部「当前态」+ 尾部两三条首行，30 秒续上；② 人复盘 → 展开每条缩进详情。

## 当前态
阶段：**S1 进行中（v2-24a 收尾：画布空白真因已修 + 后端校验层补全 + v2-31 面板缩放落盘）**。M1 核心生成链路全闭合（v2-8~11，14 文件 / 32 测试绿 / checkstyle 0 违规）；M6a v2-23 设计稿 + v2-24a 前端代码已落盘。09-16 经用户授权完成四轮修正：③ 缺 Excalidraw 样式表、前端**加载慢**（`React.lazy` 懒加载画布 + vite `optimizeDeps`/`manualChunks` 拆包）、Excalidraw 界面**英文改中文**（`langCode="zh-CN"`）；09:40~09:58 又按用户决定完成两项界面改动 + 修 3 处此前被掩盖的构建/lint 红：**去深色主题（全站固定白底）**、**删「素材库」按钮**、补回被文件锁吞掉的 `Suspense` 边界、修正 `@excalidraw/excalidraw/types/types` 类型路径、`commitToHistory`→`captureUpdate:CaptureUpdateAction.NEVER`、`while(true)`→显式标志、两处 `any`→按 `scene.schema.json` 补 `SceneElement` 类型。**前端 tsc/lint/build 三绿 + 后端 35 测试/checkstyle 0 违规**。后端起不到画布出图之间还连爆 4 个问题，已逐个定位并修：⑦ Spring `IllegalArgumentException: Name for argument ... not specified`（pom 未开 `-parameters` + `@RequestParam` 无显式名）⑧ **画布全空**（`frame` 缺 `children` 令转换器整批抛错 + 异常被吞）⑨ **手绘被 AI 结果覆盖 / 视口被抢**（`updateScene` 无合并语义）。详见尾部 11:19 与 12:40 两条。| 等待：**⓪ **SSE 120 秒超时已定位并修**（第六轮，见尾部 14:21 条）：通道上限改由生成预算推导（280s + 20s 收尾）、`LlmProvider` 新增真·总时限重载、`GenerationService` 线程兜底 + 轮次预算规划、前端中断提示；后端 **45 测试绿**；端到端实测「计算机组成原理架构图」**140 秒成功返回 25 个元素**（旧 120 秒上限必炸）。⚠️ 测试期间发现用户的 8080 后端已停止，未擅自重启** ① 用户 `npm run dev` 视觉验收 ② v2-31「可拖拽分割线」已落盘，待用户 `npm run dev` 验收 ③ 后端启动失败已修（补回本地 `application.yml` 占位 key，已被 .gitignore 忽略）；待用户填真实 Key 启动验证 chat ④ `onChange` 刷屏（缺陷#4）**已就地收敛**（400ms 防抖 + 仅回写 AI 元素 + 坐标无变化不发请求）⑤ **画布空白真因已定位并修**（绑定式箭头缺 x/y → `getCommonBounds` = NaN → 视口损坏；适配层补箭头几何 + 尺寸兜底 + `scrollToContent` 改 `fitToContent`；freedraw 安全跳过）⑥ **后端校验层补全 6 类规则**（label 对象化 / points 结构 / 枚举 / 数值范围 / 端点引用完整性 / id 唯一；GraphValidatorTest 6→14）⑦ **56+ 文件改动待用户 commit**（本轮新增 `frontend/src/lib/sceneAdapter.ts`、`frontend/src/vite-env.d.ts`、`frontend/tools/scene-smoke.mjs`、`frontend/tools/canvas-browser-check.sh`、`workbuddyFlow/tools/{run-backend.sh,run-tests.sh,sse-probe.py}`）** | 下一步：**用户**重启 dev server + 硬刷新**复验出图（本轮已端到端实测出图：三个节点 + 边缘连线箭头）→ v2-24a 收尾归档（移 `state/completed/underway/` + 更 m6a 状态行）→ S1 闭合进 S2**

## 格式
首行摘要（给恢复现场扫一眼）+ 缩进详情（给复盘展开）。每条只装一个动作；补记加 `[补]` 前缀；**每次追加顺手更新顶部「当前态」三段（阶段 | 等待 | 下一步）**。
- MM-DD HH:mm 动作摘要（一行）
  → 细节 / 证据（命令 + 结果）
  → 状态：已提交 / 待审 / 等用户 commit

## 流水（只追加）

- 09-16 09:10 纠正 activeLog 日期违规 + 复盘四拍执行偏差
  → 发现两项违规：① 今日（2026-09-16）的两条流水（08:48 v2-23/v2-24a 落盘、08:52 [补] 建 underway 计划）误写入 `2026-09-15.md`，违反 constraint.md 六.2「按自然日滚动，一天一个日志文件」与 AGENTS.md 状态机；② v2-24a 存在「先写码（08:48）后建 underway 计划（08:52 [补]）」，违反四拍第2步「人确认后才动码」。
  → 纠正：新建本文件作为今日正确日志（`2026-09-15.md` 保持不变，遵守 append-only「只追加不修改」）；后续严格按四拍：①读文档复述约束→②建 underway 计划待人确认→③动码→run-verify→追 activeLog→④收尾归档更状态行。
  → 状态：纠正中（待用户确认下一步任务）

- 09-16 09:15 排查 v2-24a 前端 dev server 代理报错（`ECONNREFUSED`；**只读诊断，未改任何文件**）
  → 现象：用户 `npm run dev`（5173）后控制台反复报 `[vite] http proxy error: /api/model/positions?sessionId=… AggregateError [ECONNREFUSED]`（`node:net`，`x25` 为 Node 对单次连接的多地址重试次数，非 25 次业务请求）；画布已能出图（渲染无碍）。
  → 根因（两层）：① **直接因**——后端 Spring Boot 未运行，Vite 代理目标 `http://localhost:8080`（`frontend/vite.config.ts` L11）无监听 → 拒连；② **更深因**——`src/main/resources/application.yml` 在工作区**已不存在**（证据：`ls -la src/main/resources/` 仅剩 `schemas/`、`templates/`；`git status --porcelain --ignored | grep '^!!'` 只列 `.idea/ .workbuddy/ HELP.md frontend/dist/ frontend/node_modules/ *.tsbuildinfo target/`，**无该文件**，而同为 ignore 的 `target/`、`frontend/node_modules/` 均可见，说明确实是文件消失而非视图屏蔽；`git log --all -- src/main/resources/application.yml` 显示其最后随 `37cba9e`「配置与规划文档本地保留，恢复密钥保护」被从库中删除）→ 而 `OpenAiCompatibleProvider` 硬依赖 `@Value("${llm.base-url}")` / `${llm.api-key}` / `${llm.model}`（**无默认值**）→ 缺配置时 Spring 启动即报占位符无法解析，**后端当前根本起不来**。
  → 附带发现（代码缺陷，属 v2-24a 范围）：`frontend/src/components/Canvas.tsx` L57 `onChange={handleChange}` → `App.tsx` L120-126 `PATCH /api/model/positions`；Excalidraw 的 `onChange` 在**任何**场景变化（挂载/选中/悬停/拖拽每帧/撤销/AI 回填）都触发 → 请求刷屏，且**页面一加载就发**（与报错时间 09:12:46 吻合）。应收敛为「坐标真变 + 拖拽结束(pointerup) + 防抖」，并只回写用户元素（`customData` 区分归属，见 `constraint.md` 一.5）。
  → 另注：`docker-compose.yml` 只传 `LLM_API_KEY` 一个 env，缺 `base-url` / `model`，即便走 Docker 也会因占位符解析失败而起不来（陈旧，待修）。
  → 历史配置结构已从 `37cba9e^` 取回（`server.port: 8080` + `llm.{provider,base-url,api-key,model,temperature,proxy,timeout-seconds}`），**api-key 已屏蔽，未落入本日志**。
  → 状态：待用户拍板（① 是否/如何恢复本地 `application.yml`；② 是否开 v2-24b 修 `onChange`；③ 后端启动后重验前端）

- 09-16 09:25 定位「中间画布与原型差距巨大」根因：**前端从未引入 Excalidraw 样式表**（只读诊断，未改任何文件）
  → 现象：截图中央是 Excalidraw 的**空场景欢迎页**（DOM 为 `welcome-screen-*` 系列），但样式全丢 → 装饰提示图标（`--help` 锁形、`--toolbar` 手形）按 SVG 原始尺寸炸成巨幅、绝对定位失效、布局错乱。
  → 证据①：`grep -rn "\.css" frontend/src/` 仅命中 `App.tsx:6 → ./App.css`、`main.tsx:4 → ./index.css`，**全仓无 `@excalidraw/excalidraw/index.css` 导入**。
  → 证据②：该包（v0.18.1）`package.json` 的 `exports` 显式暴露 `"./index.css": {production:"./dist/prod/index.css"}`（144,689 字节），而 `dist/prod/index.js`（502,077 字节）内**不含** CSS → npm/ESM 版必须显式 import（UMD 版才内联）。
  → 证据③：尺寸约束确实写在 CSS 里——`index.css` 含 `.excalidraw .welcome-screen-decor-hint--help svg{width:85px;height:71px;transform:scaleX(-1) rotate(80deg)}`；无 CSS 时 inline `<svg>` 回落到浏览器默认替换元素尺寸（约 300×150）→ 巨锁 + 巨手。
  → 证据④（对照"上次发的那个"为什么正常）：那是 PoC 原型 `excalidraw-prototype/excalidraw_collab.html`，用 **UMD 版** `excalidraw.production.min.js`（CSS 内联在 JS，`grep -c "welcome-screen-center"` = 1；目录内无独立 `.css`）→ 自带样式；且原型 `excalidraw_collab.html` L117-118 预置 `INITIAL` 示例 model、L190 `updateScene([...userElements, ...modelElements])` 立即出图。
  → 结论：差距 = ① **样式表缺失（真 bug，属 v2-24a）** + ② 原型有硬编码示例数据、新 App 为空场景（**预期行为**，等后端 M1 生成）；即修好 CSS 后画布仍会是**空**的，但会显示正常尺度的欢迎页（"Diagrams. Made. Simple." / "Pick a tool & Start drawing!"）。
  → 建议修复（一行，待用户确认后动码）：`frontend/src/main.tsx` 首行加 `import '@excalidraw/excalidraw/index.css';`，且必须置于 `import App from './App'` **之前**，以保证我方 `App.css` / `index.css` 能覆盖它。
  → 状态：待用户拍板（是否立即补这一行）

- 09-16 09:45 用户授权「给我修改好」→ 修复缺 CSS（缺陷#3）+ 一并解决前端加载慢（均属 v2-24a 范围，计划已存在、人已授权，直接改不另开计划）
  → 改动1（修 CSS）：`frontend/src/main.tsx` L1 加 `import '@excalidraw/excalidraw/index.css';`（置于 `import App` 与 `./index.css` 之前）。证据：grep 产物 `dist/assets/index-C28fc4gY.js` 命中 `.welcome-screen-decor-hint--help svg{width:85px…}` 规则 → Excalidraw 0.18 以 JS 注入样式（故不在独立 .css 文件，3KB 的 `index-DBMUX-u4.css` 仅含我方样式），样式随懒加载块生效。
  → 改动2（修加载慢）：`frontend/src/App.tsx` 把 `import Canvas` 改为 `const Canvas = lazy(() => import('./components/Canvas'))` 并用 `<Suspense fallback={<div className="canvas-loading">画布加载中…</div>}>` 包裹；`frontend/src/App.css` 补 `.canvas-loading` 占位样式；`frontend/vite.config.ts` 加 `optimizeDeps.include:['@excalidraw/excalidraw','react','react-dom']` + `build.rollupOptions.output.manualChunks` 拆 `excalidraw` / `react-vendor` 独立 chunk。
  → 验收：`npm run build` 通过（tsc 0 错误、vite exit 0）；`dist/assets/` 验证 Excalidraw 重依赖（katex/cytoscape/mermaid 图表/各 locale）已拆成 100+ 按需 chunk，首屏主包不再同步拖入整个 Excalidraw 图。
  → 说明：加载慢的根因是 `@excalidraw/excalidraw`（~500KB JS + 144KB CSS）在 `Canvas.tsx` 顶部被**静态全量引入**，首屏必须等它解析完才画；懒加载让侧栏/聊天先出、画布异步加载，消除首屏白屏与卡顿。Excalidraw 本身体积大，这是"感知提速 + 不阻塞首屏"，并非削减总字节。
  → 仍遗留未动：① `application.yml` 消失致后端起不来（环境项，非 v2-24a 代码范围，待用户恢复）；② `onChange` 直达 PATCH 刷屏（缺陷#2，越界归 M6b①，待拍板就地修 or 开 v2-24b）；③ 改动未 commit（git 由用户执行）。
  → 状态：修复完成待用户本地 `npm run dev` 视觉验收

- 09-16 09:33 用户指出「界面都是英文」→ 将 Excalidraw 界面中文化（属 v2-24a 范围，人已明示"改为中文的"，直接改）
  → 现象：截图中央右键菜单（Cut / Copy / Paste / Wrap selection in frame / Duplicate / Lock …）与左侧属性面板（Stroke / Background / Fill / Stroke width / Stroke style / Sloppiness / Edges / Opacity / Layers / Actions）均为英文。
  → 根因：Excalidraw 界面语言由 `langCode` 属性驱动，默认 `en`；`dist/types/excalidraw/types.d.ts:435` 声明 `langCode?: Language["code"]`。
  → 改动1：`frontend/src/components/Canvas.tsx` 给 `<Excalidraw>` 加 `langCode="zh-CN"`。证据：`"zh-CN"` 在 `dist/prod/index.js` 受支持语言列表内；中文语言包实体 `dist/prod/locales/zh-CN-LNUGB5OW.js`（29,287 字节）存在，构建产物含 `zh-CN-LNUGB5OW-*.js` 按需 chunk。
  → 改动2：`frontend/src/components/Sidebar.tsx` 侧栏按钮 `Library` → `素材库`。依据：Excalidraw 中文包 `zh-CN` locale 将 `Library` 译为「素材库」（grep 命中 `Library:"\u7D20\u6750\u5E93"` 等系列词条），按钮文案与其弹出的面板保持术语一致。
  → 验收：`npm run build` 绿（tsc 0 错误、exit 0）；`langCode` 值合法（tsc 类型通过）。
  → 说明：其余界面（ChartFlow 侧栏的「新建对话/历史对话」等）本就是中文，无需改。
  → 状态：待用户本地 `npm run dev` 视觉确认中文界面


- 09-16 09:40 用户提三项界面问题 -> 只读勘察 + 问答定向，拆成「两项就地修 + 一项新功能立计划」
  -> 用户原话三项：①「界面风格切换的问题」②「左边的素材库不要了」③「三大区域不能用分割线拖拽来改变大小」。
  -> 只读勘察（截图按真实尺寸 1912×948 放大核对）：当前为**深色模式**（左下按钮显示「☀️ 浅色」，即 `isDark=true`），左右两栏 `#111827` 深蓝、中间画布仍纯白；`grep -rn "resize|splitter|drag|pointerdown" frontend/src/` **零命中** -> 三栏目前是死宽（`.sidebar{width/min-width:240px}`、`.chat-panel{width/min-width:340px}`），PoC 原型 `excalidraw_collab.html` L11 同样是死宽（`#chat{width:340px;min-width:340px}`）-> **从来没有过可拖拽分割线**。
  -> 顺带发现真 bug：`App.css` 深色块里 `.app.dark .msg.agent` / `.msg.user` 只改背景不改文字色（文字仍 `#1F2937` 深灰）-> 深底深字基本不可读。
  -> 问答定向（AskUserQuestion 两问）：① -> 用户选「干脆不要深色主题」；③ -> 用户选「要能拖拽调整宽度」。
  -> 状态：按用户选择拆解——①（去深色主题）+②（删素材库）直接在 v2-24a 内改；③ 单开 `plans/underway/v2-24b-panel-resize.md` 待人确认。

- 09-16 09:58 v2-24a 收尾改动落盘：去深色主题 + 删「素材库」按钮 + 修 3 处被掩盖的构建/lint 红
  -> ① 去深色主题（全站固定白底）：`App.tsx` 删 `isDark` state 与 `app light/dark` 动态类名；`Sidebar.tsx` 删底部主题切换按钮及 `onToggleTheme`/`isDark` 入参；`App.css` 删整个「深色主题」块 + `.btn-theme`/`.sidebar-footer`；`index.css` `color-scheme: light dark`->`light`（防系统深色模式把滚动条/输入框变深）；`Canvas.tsx` 加 `theme="light"` 锁定画布浅色。
  -> ② 删「素材库」按钮：`Sidebar.tsx` 删按钮与 `onToggleLibrary` 入参、`App.tsx` 删对应传参、`App.css` 删 `.btn-library` 两条规则。说明：Excalidraw 自带素材库面板仍可从画布**右上角按钮**打开，功能未消失，只是不再占用侧栏。
  -> ③ 补回被文件锁吞掉的 `Suspense` 边界：`App.tsx` L1 已 import `Suspense` 但 JSX **从未使用**，而 `Canvas` 是 `lazy` -> 首屏渲染缺兜底边界会抛错白屏（`.canvas-loading` 样式早已落盘却一直无人引用）；已在 `<Canvas>` 外包 `<Suspense fallback={<div className="canvas-loading">画布加载中…</div>}>`。
  -> ④ 修 3 处此前被「管道吞退出码」掩盖的构建红（09:34 起 `tsc -b` 实为红，前几次「构建通过」的结论有误）：
    · `Canvas.tsx` 类型导入 `@excalidraw/excalidraw/types/types` -> **`@excalidraw/excalidraw/types`**（包 `exports` 只把 `./*` 映射到 `dist/types/excalidraw/*.d.ts`，且主入口 `index.d.ts` 并不导出 `ExcalidrawImperativeAPI`；原写法 TS2307）。该错误把 `api` 类型退化成 `any`，**掩盖了下一条**。
    · `api.updateScene({ commitToHistory: false })` -> **`captureUpdate: CaptureUpdateAction.NEVER`**（0.18.1 已移除 `commitToHistory`，TS2353；`NEVER` 语义＝远端/初始化更新不入 undo 栈，与旧写法等价）。
    · 删除 `Canvas.tsx` 未使用的 `useRef` 导入（TS6133，`noUnusedLocals`）。
    · 3 个 eslint error：`App.tsx` `while (true)`（no-constant-condition）-> 改显式 `streamDone` 标志；`Canvas.tsx` 两处显式 `any` -> 按后端 `src/main/resources/schemas/scene.schema.json` 契约在 `types.ts` 新增 `SceneLabel`/`SceneElementType`/`SceneElement`（**后端 `label` 契约为对象 `{text}`，不是字符串**），`elements` 全链路改 `SceneElement[]`，`convertToExcalidrawElements` 入参改用类型断言。
  -> 证据：`npx tsc -b` **exit=0**（无输出）；`npm run lint` **exit=0**；`npm run build` **exit=0**（`✓ built in 15.53s`）；`grep -rn ": any" src/` 无命中；后端门禁等价步骤（`java -classpath …plexus-classworlds-2.7.0.jar … Launcher -B clean test`）-> `Tests run: 32, Failures: 0, Errors: 0, Skipped: 0` + `BUILD SUCCESS`（exit 0）；`… Launcher -B checkstyle:check` -> `You have 0 Checkstyle violations.` + `BUILD SUCCESS`（exit 0）。
  -> 未跑：门禁 2/3 里的 `npm install`（触网，未获授权）；该步对应的 `npm run build` / `npm run lint` 已用本地 node_modules 原样跑通。
  -> 发现待人拍板（`run-verify.ps1` 健壮性）：L44-49 `Get-MavenHome()` 里 `(Join-Path $env:ProgramFiles …)` 在 `$env:ProgramFiles` 空/未设时抛参数量化错，**中断整个候选数组构造** -> 连第一候选 `D:\maven-home\apache-maven-3.9.5-bin\apache-maven-3.9.5`（实际存在）都不再检查，直接 `VERIFY FAIL: 未找到 Maven 安装`；本次即因该环境缺省而 FAIL（补 `$env:MAVEN_HOME` 后可通过）。建议加判空或 `-ErrorAction SilentlyContinue`。另：L68 `& java` 依赖 PATH 中的 java，本会话需手工补 `C:\Users\22719\.jdks\ms-17.0.17\bin`。
  -> 状态：等用户本地 `npm run dev` 视觉验收（预期：整站白底无深色；左栏只剩 ChartFlow / + 新建对话 / 历史对话；画布浅色 + 中文界面）。

- 09-16 09:58 立 v2-24b「三栏可拖拽分割线」计划（**状态：待审，人确认前不动码**）
  -> 产出：`plans/underway/v2-24b-panel-resize.md`（六段齐全：目标/边界/分层边界/前置/步骤/验收口径/覆盖测试数据 + 待拍板项）。
  -> 关键取舍已写入计划：面板宽度属**纯 UI 偏好**，**不进后端 IR / 真相源**（架构不变式：图数据真相源在后端，宽度不是图数据）；左右栏各设上下限；拖拽用 `pointerdown/pointermove/pointerup` + `setPointerCapture`（不用 mouse 事件，避免拖出窗口丢事件）。
  -> 待拍板：上下限取值（建议左 200–420 / 右 280–600）、是否 localStorage 记忆宽度（建议先不做）、是否双击复位（建议先不做）。
  -> 状态：待审。

- 09-16 10:26~10:44 修复后端启动失败：补回缺失的本地 `src/main/resources/application.yml`
  -> 现象：用户本地启动报 `Error starting ApplicationContext` / `Application run failed`。
  -> 根因：`src/main/resources/` 下已无任何 yml/properties（`application.yml` 此前随 `37cba9e`「本地保留、恢复密钥保护」被删，本地磁盘亦已不存在）；`OpenAiCompatibleProvider` 的 `llm.base-url` / `llm.api-key` / `llm.model` 三处 `@Value` **无默认值**（proxy/timeout 有默认值），Spring 无法解析占位符 -> 容器启动即失败。
  -> 修复：重建本地 `application.yml`（`server.port=8080` + `llm.{provider,base-url,api-key,model,proxy,timeout-seconds}`），`api-key` 用**占位符** `请配置你的真实APIKey`（**不写入真实密钥**）；该文件已被 `.gitignore` L40 忽略，用户后续填真实 Key 也不会被提交/推送（符合"密钥保护"意图）。
  -> 证据：`… Launcher -B clean test` -> `Tests run: 32, Failures: 0, Errors: 0` + `BUILD SUCCESS`（exit 0）；`… Launcher -B checkstyle:check` -> `You have 0 Checkstyle violations` + `BUILD SUCCESS`（exit 0）。注：单测不启 Spring 容器，yml 对单测无影响；真正启动验证需用户本地 `mvn spring-boot:run` 并填真实 Key。

- 09-16 10:27~10:44 实现「三栏可拖拽分割线」（编号纠错 v2-24b -> v2-31 并归档）
  -> 编号纠错：原草稿 `plans/underway/v2-24b-panel-resize.md` 与 masterPlan `m6b-interaction.md` 已占用的 `v2-24b`（拖拽画布元素回写坐标，M6b①/S3）**撞号**；本"面板缩放"功能改用 **v2-31**，归档至 `state/completed/underway/v2-31-panel-resize.md`（草稿已删）。
  -> 改动：`App.tsx` 加 `leftWidth`(240)/`rightWidth`(340) state + `dragRef` + `onSplitterDown/Move/Up`（pointer 事件 + `setPointerCapture`，拖出窗口不丢事件；`DRAG_LIMITS` 左[200,420]/右[280,600]），`Sidebar`/`ChatPanel` 间各插一道 `.splitter`；`App.css` 加 `.splitter`（6px/`col-resize`/hover 高亮蓝/`touch-action:none`），移除 `.sidebar` 与 `.chat-panel` 原 border（分隔视觉改由分割线承担）；`Sidebar.tsx`/`ChatPanel.tsx` 接口加 `style?: React.CSSProperties` 并透传宽度。
  -> 工具坑（已记录）：**同一文件的多个 Edit 放同一条消息，只有最后一个落盘**（本次 App.tsx 四改只落 JSX、Sidebar/ChatPanel 的 interface 改被吞、activeLog 三改只落最后一改），全部改为单条顺序补回。
  -> 证据：`npx tsc -b` exit 0；`npm run lint` exit 0；`npm run build` exit 0（`✓ built in 14.38s`，仅 Excalidraw 体积提示非错误）。
  -> 验收：待用户 `npm run dev` 视觉确认（悬停 `col-resize`、左右分割线各自改对应栏、clamp 生效、移出窗口仍可拖、画图/对话不受影响）。

- 09-16 11:13~11:16 修复 LLM `400 Unsupported model mimo-v2.5s`（后端可启动后暴露的第二个配置错）
  -> 现象：后端已起（yml 修复生效）+ 用户填入真实 Key 后，`POST /api/chat` 报 `java.lang.RuntimeException: LLM 调用失败: LLM API 返回错误码 400`，响应体 `{"error":{"code":"400","message":"Unsupported model mimo-v2.5s"}}`。
  -> 关键判读：报 **400 而非 401** -> 接口可达、**Key 鉴权已通过**（Key 有效），问题只在 `model` 字段写了一个不存在的型号。
  -> 根因：`llm.model: mimo-v2.5s` 型号不存在。Xiaomi MiMo 官方文档：**MiMo-V2 系列（`mimo-v2-pro`/`mimo-v2-omni`/`mimo-v2-flash`/`mimo-v2-tts`）已于 2026-06-30 00:00 正式下线**，须切 V2.5 系列；`-v2.5s` 这种简写从未存在。
  -> 定源（实测而非猜）：用用户自己的 Key 直连只读端点 `GET https://api.xiaomimimo.com/v1/models` -> HTTP 200，账号真实可用清单 = `mimo-v2.5` / `mimo-v2.5-pro` / `mimo-v2.5-asr` / `mimo-v2.5-tts` / `mimo-v2.5-tts-voiceclone` / `mimo-v2.5-tts-voicedesign`（可作文本生成的仅前两个）。
  -> 修复：`src/main/resources/application.yml` 的 `model` 由 `mimo-v2.5s` 改为 **`mimo-v2.5-pro`**（官方定位"文本生成 / 深度思考 / 流式 / Function Call / 结构化输出 / 联网搜索，1M 上下文、最大输出 128K"，最贴合本项目 ReAct + Diagram Tools + 结构化输出的主循环场景；替代项 `mimo-v2.5` 能力接近、计费 1x 更省，已在 yml 注释中列出）。`base-url`/`api-key` 未动（`base-url` 用的是**完整** `.../v1/chat/completions`，与代码 `URI.create` 直用一致，正确；Key 为 `sk-` 前缀=支付即付，对应 `api.xiaomimimo.com` 端点亦正确，Token Plan 才需换 `token-plan-{cn,sgp,ams}.xiaomimimo.com/v1`）。
  -> 冒烟证据（复刻 Java 请求体，仅 `model`+`messages`）：`curl -X POST .../v1/chat/completions -d '{"model":"mimo-v2.5-pro","messages":[{"role":"user","content":"只回复两个字：你好"}]}'` -> **HTTP 200**，`choices[0].message.content = "你好"`，`model: mimo-v2.5-pro`，usage `total_tokens=285`（含 `reasoning_tokens: 25`，该型号为推理模型，响应含 `reasoning_content` 字段——现有 `extractContent` 只取 `content`，不受影响）。
  -> 连带处理：`target/classes/application.yml`（构建产物）仍是旧 `mimo-v2.5s`，若用户走 IDE 增量编译重启可能读到旧值；已以 `… Launcher -B clean test` 重建（该 phase 会重跑 `process-resources`），核对 `target/classes/application.yml:13 -> model: mimo-v2.5-pro`。
  -> 门禁证据：`clean test` -> `Tests run: 32, Failures: 0, Errors: 0` + `BUILD SUCCESS`（exit 0）；`checkstyle:check` -> `BUILD SUCCESS`（exit 0）。
  -> 遗留提示（未擅自改，待用户拍板）：① `llm.timeout-seconds: 60` 对推理模型 + 大图 prompt 可能偏紧（可考虑 120）；② `OpenAiCompatibleProvider.chat()` 不解析 `tool_calls`——M4（ReAct + Diagram Tools）落地时需扩展；③ 该 yml 含用户真实明文 Key，已确认被 `.gitignore` L40 忽略，**仍未 commit**。

- 09-16 11:19~11:35 修 Spring 启动后暴露的第三个问题：`PATCH /api/model/positions` 500 + 画布空（[补]）
  -> 现象 A（后端 500）：每次前端调 PATCH 即报 `java.lang.IllegalArgumentException: Name for argument of type [java.lang.String] not specified, and parameter name information not found in class file either.`，栈顶 `AbstractNamedValueMethodArgumentResolver.updateNamedValueInfo`。
  -> 根因 A：`DiagramController.updatePositions(@RequestParam String sessionId, ...)` **没写显式参数名**，而 class 文件里**没有**参数名信息 -> Spring 解析不了。为什么 class 里没有参数名：`pom.xml` **没有继承 `spring-boot-starter-parent`**（改用 `dependencyManagement` 手动 import），父 POM 默认给的 `maven.compiler.parameters=true` 因此丢失，`javac` 未带 `-parameters` 编译。
  -> 修复 A（双保险）：① `pom.xml` 的 `maven-compiler-plugin` 显式加 `<parameters>true</parameters>`；② `@RequestParam(name = "sessionId")` 写死名字。
  -> 验证 A：`javap -p -v target/classes/.../DiagramController.class` 里 **`MethodParameters` 块存在**（`sessionId`/`positions`/`request` 均在）-> 证明 `-parameters` 真进了 class 文件，不是只改了配置。
  -> 现象 B（前端画布空）：聊天显示"已生成 15 个元素"，画布一片空白。
  -> 根因 B：`Canvas.tsx` 骨架映射漏搬 `text` 元素的 `text`/`fontSize` 与 `arrow` 的 `start`/`end`；`convertToExcalidrawElements` 对 text 元素直接 `.replace()`，拿到 undefined 即抛 `TypeError: Cannot read properties of undefined (reading 'replace')`，且是**整批**失败；异常又被 `catch` 静默吞掉 -> 画布全空。
  -> 验证 B（探针实测，非推断）：A 当前映射 -> THREW；B 补 `text`/`fontSize` -> PASS 9 元素；C 再补 `start`/`end` -> PASS 9 元素；E 仅 text 无 `text` 字段 -> THREW。另实测 `{ regenerateIds: false }` 下后端 id 完整保住（`["start","input","check_input",...]`）且箭头绑定正确（`startBinding.elementId = "check_input"` / `endBinding = "input_invalid"`）；默认 `true` 则 id 全随机、绑定全丢。
  -> 连带发现（测试空洞）：`DiagramControllerTest` 只测 `/api/health` 与两条 404 防回归，**`POST /api/chat` 和 `PATCH /api/model/positions` 一条测试都没有** -- 所以"参数名没写"这种 500 能一路漏到运行期。
  -> 补测：新增 3 条（`/api/chat` 缺 message 返回 400；`/api/chat` 正常开启 SSE 流；`PATCH /api/model/positions` 正常 200 且入参抵达 `SessionStore`）。**后端测试 32 -> 35**。
  -> 附带查清（类型契约瑕疵，非崩溃）：经该端点到达 `SessionStore` 的坐标数值是 **Integer** 而非接口声明的 `Double`。用 Jackson 直连对照实验确认：`Map<String, Map<String, Double>>` -> Double；裸 `Map<String,Object>` -> Integer，即该端点泛型未参与反序列化。`InMemorySessionStore` 按 `Object` 存取、不做数值假设，故**不会崩**；测试改为断言真实行为并在注释留痕。
  -> 门禁证据：`clean test` -> `Tests run: 35, Failures: 0, Errors: 0` + `BUILD SUCCESS`；`checkstyle:check` -> 0 违规 + `BUILD SUCCESS`；前端 `tsc -b`/`eslint`/`vite build` 三者 exit 0。

- 09-16 12:40~13:00 修画布三个交互缺陷：出图空白 / 手绘被吞 / 视口被抢（第四轮）
  -> 用户反馈：「前端生成了没东西呀图形怎么没看到」「我准备手画结果又跳转到另外一个画布去了」「我先画一个图案然后再生成结果之前的不见了」。
  -> 定位手段：先用 Pillow 裁切放大截图分三区读清（左栏**与磁盘代码一致**，排除旧版本误读；聊天区"已生成 10 个元素"；**画布区纯白**）-> 确认症状是"`setElements` 执行了，但一个元素都没渲染出来"。
  -> 根因 1（画布空白，探针矩阵实测）：LLM 输出里只要出现 **`frame` 元素**，`convertToExcalidrawElements` 即抛 `TypeError: Cannot read properties of undefined (reading 'forEach')`（转换器对 frame 强制访问 `children.forEach`）。**而 `scene.schema.json` 的 frame 分支是 `additionalProperties:false` 且没有 `children` 字段** -- 即"契约里的 frame"与"转换器要的 frame"天生不兼容，frame **永远渲染不出来**；又因整批失败 + 异常被吞，表现为"生成了但画布全空"。矩阵证据：`frame 无 children` -> THROW；`frame children=[]` -> PASS；`text 缺 text` -> THROW(`replace`)；`10 元素无 frame` -> PASS 16；`同 10 元素 + 一个 frame` -> THROW。
  -> 根因 2（手绘被吞 + 视口被抢，两者同源）：`Canvas.tsx` 收到新 IR 时执行 `updateScene({ elements: <AI 元素> })`，**语义是替换整个场景** -- 用户手绘元素被直接吃掉；同时**无条件** `scrollToContent`，把用户正准备作画的视口夺走（用户描述为"跳转到另外一个画布"）。
  -> 修复 1：抽出适配层 `frontend/src/lib/sceneAdapter.ts`（纯逻辑、可测），在适配层给 `frame` 补 `children: []`（**不动契约**，后端不泄露 Excalidraw 内部结构）；并加**逐元素隔离**降级：整批失败 -> 逐元素试转定位"毒元素"并记 id -> 幸存者再整批转换（保住箭头绑定）-> 全败才 fatal。
  -> 修复 2（场景所有权）：AI 元素打 `customData.chartflowModel = true`；收到新 IR 时 `elements = [...AI元素, ...用户元素]`，**只替换带标记的那部分**；「新建对话」（IR 为空）只摘掉 AI 元素、保留用户手绘。
  -> 修复 3（视口取景）：`scrollToContent` 只在「新一轮生成（AI 元素 id 签名变化）」时执行一次，且**取景框 = AI 元素 ∪ 用户手绘**（只框 AI 元素会把用户图案留在框外，观感就是"跳到另一个画布"；完全不框又会让 AI 图落在视口外，退化成"生成了看不到"）。
  -> 修复 4（缺陷#4 就地收敛）：`onChange` 改为 **400ms 防抖 + 仅回写带 AI 标记的元素 + 坐标签名无变化不发请求**，卸载时清理计时器 -- 原实现是任何场景变化（挂载/选中/悬停/拖拽每帧/撤销）都全量 PATCH。
  -> 修复 5（不再静默）：新增 `onRenderIssue` 上报口，`App.tsx` 去重后把"无法渲染的元素 id / 整体失败原因"作为 error 消息显示在聊天区 -- 画布空白本身没有任何解释能力，这类失败必须可见。
  -> 连带固化（可复用回归）：新增 `frontend/tools/scene-smoke.mjs`（`node tools/scene-smoke.mjs`，**不新增依赖**，esbuild 由 vite 传递带入；用 DOM 桩作 esbuild banner 让 Excalidraw 能在 Node 里加载），把"某个元素形状让转换器整批抛错"固化成 6 条用例。实测 6/6 PASS（含 `frame 无 children` 与 `text 缺 text` 两条历史翻车场景）。
  -> 编号说明：本轮**不新开计划**，作为 v2-24a 验收期缺陷（缺陷 8/9）并入 `plans/underway/v2-24a-canvas-embed.md`。
  -> 门禁证据：前端 `tsc -b` exit 0 / `eslint`（与 lint 脚本同参数）exit 0 / `vite build` exit 0（19.15s，仅体积提示）；后端 `clean test` -> `Tests run: 35, Failures: 0, Errors: 0` + `BUILD SUCCESS`；`checkstyle:check` -> 0 违规 + `BUILD SUCCESS`；`node tools/scene-smoke.mjs` -> `SMOKE PASSED: 6/6`（exit 0）。
  -> 未跟踪文件核对（交付红线）：`git status --porcelain -uall` = 56 条；本轮新增两条未跟踪文件 `frontend/src/lib/sceneAdapter.ts`、`frontend/tools/scene-smoke.mjs` **均已出现在列表中**；冒烟脚本产物落在 `frontend/node_modules/.cache/`（已被 `frontend/.gitignore:1` 忽略，不会污染工作区）。
  -> 遗留（未擅自改）：① `run-verify.ps1` 的 `Get-MavenHome()` 在 `$env:ProgramFiles` 为空时会抛错并连带跳过合法候选（本沙箱因此误报 "Maven not found"），建议加空值守卫；② `llm.timeout-seconds: 60` 对推理模型偏紧；③ `frame` 在契约里存在但语义不完整（无 `children`），是否改为"后端显式要求 `children: []`"或"契约直接去掉 frame"，待拍板。

- 09-16 13:12~14:06 修「生成后画布空白」真因：绑定式箭头缺坐标 -> 取景边界 NaN（第五轮，用户要求"1，2都改 + 你自己画图试试"）
  -> 用户反馈：整页白屏截图 + 「还是原来的情况，还会出现空白的情况，执行完之后你测试一下你直接画图试试看看有没有问题」。
  -> 先排除"整页白屏"：真实浏览器实测当前代码在 dev 下**完全正常**（`hasApp:true` / `sidebar:true` / `splitter:2` / `canvas:true` / `errs:[]`，bodyText 含 ChartFlow/新建对话/历史对话/形状/发送），模块请求全部 HTTP 200 无编译错误，`dist/dev` 与 `dist/prod` 的 `./index.css` 条件导出齐备 -> 判断用户看到的是**旧 dev server / 浏览器旧状态**（`vite.config.ts` 改过 `optimizeDeps`，必须重启 dev server 才生效）。
  -> 定位真因（新增 dev 探针后一次问清）：`Canvas.tsx` 挂 `window.__chartflow`（仅 `import.meta.env.DEV`），可直接读 `getSceneElements()`/`getAppState()`、用 `getCommonBounds()` 算取景边界、用 static canvas 非白像素判断"到底画出来没有"。
    · 症状证据：聊天区"已生成 5 个元素"，画布空白，且画布上只剩**「滚动回到内容」**按钮（= 元素在场景里但落在视口外）。
    · 根因：LLM 输出绑定式箭头 `{id:"arrow1",type:"arrow",start:{id:..},end:{id:..}}` **不带 x/y**（契约里 arrow 的 x/y 可选），而 Excalidraw 的 skeleton 要求 arrow 必须有 x/y -> 转换产物 `x=undefined` -> `getCommonBounds` = `[NaN,NaN,NaN,NaN]` -> `scrollToContent` 用 NaN 算视口 -> 视口损坏 -> 元素全被画到视口外。
    · 实测 A：`nullXY=["arrow1|arrow|x=undefined,y=undefined","arrow2|arrow|x=undefined,y=undefined"]`、`bounds=["NaN","NaN","NaN","NaN"]`、`scrollBtn=true`、static canvas 非白像素 **0**。
    · 实测 B（对照）：仅把 x/y 补 0，`bounds=["0.5","-1.5176","720","240"]`、`scrollBtn=false`、非白像素 **18843**。
    · 必然性：提示词明确要求"箭头优先用 start.id/end.id 绑定" -> **每次生成都会踩**，非偶发。
  -> 修复 A（适配层补几何）：`sceneAdapter` 新增 `withArrowGeometry` —— 绑定式箭头缺几何时用被绑定元素推导：起点 = 起点元素**边界**朝终点方向的交点，`points=[[0,0],[终点-起点]]`。用边界交点而非中心点，是因为中心点会让箭头**穿过图形、与框内 label 文字重叠**（第一次修复后的截图实测发现，遂改成边缘交点）。
  -> 修复 B（尺寸兜底，NaN 第二来源）：`freedraw` 转换后 `width/height` 为 `undefined`（其余类型转换器会自己算），`x + width` 同为 NaN；`ensureFiniteSize` 按 `points` 包围盒补尺寸，x/y 一并兜底。
  -> 修复 C（`scrollToContent` 参数）：原代码传 `fitToViewport: true`，而 Excalidraw 0.18 的 opts 是**互斥联合**（`{fitToContent?; fitToViewport?: never}` | `{fitToViewport?; fitToContent?: never}`，见 `components/App.d.ts:335`），`fitToViewport` 是"适配 frame 视口"专用分支；改 `fitToContent: true` 并延后一帧（等 `updateScene` 提交完再算 bounds），卸载时取消 rAF。
  -> 附带（契约 vs 渲染器，同类缺口第二例）：`freedraw` 的取景边界在 Excalidraw 里算不出来（points 合法、x/y/w/h 齐全，`getCommonBounds` 仍 NaN）。AI 生成图表本不该输出手绘路径，故：适配层跳过 freedraw（用户手绘不经此路径，不影响手绘）、`PromptService` 明确禁止输出、`PromptServiceTest` 改断言"7 种可生成 type + 明确禁用 freedraw"。
  -> 后端源头治理（用户所指第 2 条）：`GraphValidator` 补 6 类此前完全没校验的规则 —— `checkLabel`（必须是 `{text}` 对象且 text 非空）、`checkPointsShape`（每点必须是 2 个数字）、`checkOptionalEnum`（fillStyle/strokeStyle/textAlign/fontFamily）、`checkOptionalPercent`（opacity 0~100）、`checkOptionalMin`（fontSize ≥1、strokeWidth/roughness ≥0）、`checkBinding`（start.id/end.id 必须指向本场景已存在元素）+ id 唯一性；实现改两遍扫描（先收 id 全集再校验引用）。依据：22 用例矩阵实测这些畸形数据**不抛错但可见地坏掉**（label 字符串静默丢文字 / points 非数字产生 NaN 坐标 / 端点悬空让箭头失去跟随）。
  -> 新增可复用工具：`frontend/tools/canvas-browser-check.sh`（浏览器端画布体检，不依赖后端与 LLM：注入典型场景后查 bounds、视口、绘制像素，输出 `PASS|FAIL`；自检 PASS）。同步清理本轮 9 个一次性诊断脚本（`browser-*.sh`，功能已并入上述工具），并把 `run-backend.sh`/`run-tests.sh`（沙箱里 maven 命令会坏，改用 java 直启 launcher 的姿势）归置到 `workbuddyFlow/tools/`。
  -> 门禁证据：前端 `tsc -b` exit 0 / `eslint`（同 lint 参数）exit 0 / `vite build` exit 0；后端 `clean test` -> `Tests run: 43, Failures: 0, Errors: 0` + `BUILD SUCCESS`（GraphValidatorTest 6->14）；`checkstyle:check` -> `You have 0 Checkstyle violations.` + `BUILD SUCCESS`；`node tools/scene-smoke.mjs` -> `SMOKE PASSED: 10/10`（6->10 用例）。
  -> 端到端实测（真实浏览器 + 真实 LLM；后端 8080 与前端 5173 均为用户已起的进程，未擅自杀进程）：场景 A（纯生成）`sceneCount=8`、`types=rectangle*3,arrow*2,text*3`、`bounds=[100,150,620,200]` 无 NaN、`nonFiniteCoords=[]`、`scrollBtn=false`、static canvas 非白像素 `0 -> 20383`，**截图呈现三个节点 + 两条边缘连线箭头**；场景 B（先放用户元素再生成）`userMarkerKept=true`、`scrollBtn=false` —— 手绘保留、视口不抢。
  -> 状态：待用户本地 `npm run dev` 复验（若页面仍旧态，需**重启 dev server + 硬刷新**）；改动未 commit（git 由用户执行）。

- 09-16 14:21~14:45 修「SSE 通道 120 秒超时掐断 + 生成线程裸崩」（第六轮）
  -> 用户反馈：贴出后端日志 `AsyncRequestTimeoutException` + `Exception in thread "Thread-2" java.lang.IllegalStateException: ResponseBodyEmitter has already completed`，问"这个算是超时了吗"。
  -> 判定：**是超时**，证据确凿 —— 14:18:22.424（生成轮次 1/3）到 14:20:22.749（超时告警）= **120.325 秒**，与 `DiagramController` 里写死的 `new SseEmitter(120_000L)` 严丝合缝（偏差 0.3 秒 = 定时器调度精度）。
  -> 直连实测（`curl -w` 打点，同量级需求）：`ttfb=8.219416` / `total=97.681223` / `code=200` / `bytes=17481` —— **单轮 LLM 就要约 100 秒**，而 120 秒上限要覆盖最多 3 轮，第 2 轮必然越界。
  -> 隐蔽缺陷（本轮新发现）：`llm.timeout-seconds: 60` **是假闸门**。首字节 8.2 秒就返回（服务端推理期间先发空白占位，用户日志里那一片空行即证据），此后 JDK 的请求计时不再生效，正文又传了 89 秒也没被中断。
  -> 因果链（与用户日志逐行对应）：① 第 1 轮 `thinking` 发出 -> 响应流开启（`response committed`）② LLM 算满 120 秒 ③ 容器按 emitter 超时掐断 -> `AsyncRequestTimeoutException`，因响应已提交只能 `Ignoring exception`（连错误都发不出去）④ 前端拿不到任何事件，流静默中断 ⑤ LLM 随后返回 200（数据白生成）⑥ 代码不知情继续执行，`sendEvent` 抛 `IllegalStateException` -> **Thread-2 裸崩** ⑦ 前端 `reader.read()` 读到 EOF，`finally` 只关掉 loading、不给任何提示。
  -> 修复 1（预算对齐，治本）：`GenerationService` 新增 `public static final long TOTAL_BUDGET_MS = 280_000L`，`DiagramController.SSE_TIMEOUT_MS` 由它推导（`TOTAL_BUDGET_MS + 20_000`）—— 两者恒等对齐，不再各写各的魔数。
  -> 修复 2（轮次预算规划）：新增 `planRound(deadline)`，本轮上限 = `min(150 秒, 剩余预算 − 20 秒收尾余量)`；低于 10 秒直接如实报错收尾（"生成超时：已耗时 N 秒，剩余时间不足以完成第 N 轮"），不再开着新一轮去撞墙。
  -> 修复 3（真·总时限）：`LlmProvider` 新增 `chat(prompt, timeoutSeconds)` 重载；`OpenAiCompatibleProvider` 改用 `sendAsync(...).get(timeout)` 在 Future 层加硬时限，覆盖"连接 + 响应头 + 正文"全过程（原 `HttpRequest.timeout()` 只够覆盖到首字节）；并新增耗时日志 `=== LLM HTTP 状态码: 200，耗时 N ms`。
  -> 修复 4（线程兜底）：`generate()` 包 `try-catch (RuntimeException)`；`sendEvent`/`sendError` 增补 `catch (IllegalStateException)`（通道已结束时的真实异常类型是 `IllegalStateException` 而非 `IOException`），不再裸崩。
  -> 修复 5（前端不再沉默）：`App.tsx` 新增 `settledRef`，流读完却未收到 `result`/`error` 时明确提示"生成中断：连接在返回结果前就断开了……请重试，或把需求拆成更小的图"。
  -> 配置：`application.yml` 的 `llm.timeout-seconds` 由 60 上调至 150（依据实测单轮约 100 秒）。
  -> 门禁证据：后端 `test`（**刻意未用 `clean`**，避免删 target 影响用户运行实例）-> `Tests run: 45, Failures: 0, Errors: 0` + `BUILD SUCCESS`（新增 `survivesEmitterAlreadyCompleted`、`reportsErrorWhenLlmFails`）；`checkstyle:check` -> `You have 0 Checkstyle violations.`；前端 `tsc -b` / `eslint` / `vite build` 三 exit 0 + `node tools/scene-smoke.mjs` -> `SMOKE PASSED: 10/10`。
  -> 端到端实测（新代码另起 8081 端口跑 `spring-boot:run`，未动用户进程）：同一句"画一个计算机组成原理架构图…"，`thinking`(0.11s) -> `validation`(140.15s) -> `result`(140.17s)，**25 个元素成功返回**；8081 日志 `Exception in thread` 计数 **0**、`AsyncRequestTimeoutException` 计数 **0**。**140 秒 > 旧上限 120 秒** —— 同一需求旧代码必炸、新代码通过，因果闭环。测试实例已 `Stop-Process` 清理。
  -> ⚠️ 交付后核查发现：测试结束后 **8080 上用户的后端已停止**（直连返回 `000`、无监听）。同时段我执行过 `mvn test`（会重写 `target/classes`），若用户的实例是 `spring-boot:run` 形态读 `target/classes`，理论上存在懒加载类被替换的干扰可能。**已如实告知用户，未擅自重启或触碰其进程**。
  -> 状态：代码与门禁完成；待用户重启后端 + dev server 复验。

- 09-16 14:40~15:20 「复杂图必失败 + 报错后界面卡死」两项缺陷（第七轮，用户要求"让他画一些复杂的看看"）
  -> 复现步骤：真实浏览器（agent-browser）+ 真实 LLM，输入 5 层微服务电商架构需求（接入层/业务服务层/中间件层/数据层/运维层）。
  -> 缺陷 1（复杂图被掐断）：14:43:02 开始第 1 轮 -> 14:45:32 报错 = **150.3 秒**，正是 `MAX_ROUND_SECONDS = 150` 这道闸门。前端只看到"LLM 调用失败: LLM 调用超时"，画布全空（sceneCount=0、canvasNonWhite=0）。
  -> 缺陷 2（报错后界面永久卡死，比缺陷 1 更严重）：错误信息确实推到了前端，但**输入框和发送按钮一直保持 disabled** —— 实测从第 110 秒一直卡到第 390 秒（`isLoading` 始终为 true），用户除了刷新页面没有任何出路。
     根本原因：`sendError` 调用 `emitter.completeWithError(...)`。错误内容已经作为 `error` 事件推给客户端了，通道层再"报错"会让容器去走 ERROR dispatch，而响应头此时是 `Content-Type: text/event-stream`，容器找不到能把错误体写成 SSE 的消息转换器，日志实证抛 `HttpMessageNotWritableException: No converter for [class java.util.LinkedHashMap] with preset Content-Type 'text/event-stream'`。后果是响应既不收尾也不断开，前端 `reader.read()` 永远等不到 `done`。
  -> 修复 1（后端流收尾）：`GenerationService.sendError` 由 `completeWithError(...)` 改为 `complete()`（错误已用 error 事件表达，通道层无需再报错）。回归锁：`GenerationServiceTest` 两条用例加 `verify(emitter, never()).completeWithError(any())`。
  -> 修复 2（前端不再依赖服务端收尾）：`App.tsx` 在收到终态事件（result/error）后立即 `await reader.cancel()` 并退出读循环，不再傻等 `done`。服务端行为再反常也最多是"多发一次，前端照样解锁"。
  -> 修复 3（预算由实测重定）：`MAX_ROUND_SECONDS` 150 -> **300**，`TOTAL_BUDGET_MS` 280s -> **420s**（SSE 上限随之自动推导为 440s）。
  -> 修复 4（等待可见化）：`ChatPanel` 新增纯前端计时，显示"正在生成... 已等待 N 秒"，超过 90 秒附一句"复杂架构图本轮实测需 100~260 秒"。不引入服务端心跳，零协议改动。
  -> 端到端复测（新代码，真实浏览器 + 真实 LLM，两次都一次通过、无重试）：
     · 场景 c2「微服务电商系统架构图（5 层）」：LLM **50.280s** / completion 4207 tokens -> **`已生成 47 个元素`**；场景 66 个元素（19 rectangle + 5 frame + 23 arrow，另 19 个 text 为 Excalidraw 自动生成的标签），`bounds=[-10,-10,1490,680]` 有限、`nonFiniteXY=0`、`hScrollBtn=false`、`canvasNonWhite=37391`、浏览器错误 `[]`。
     · 场景 c3「电商下单支付流程图（含三条支付分支 + 两条异常分支）」：LLM **110.996s** / completion 6649 tokens -> **`已生成 41 个元素`**；场景 67 个元素（16 rectangle + 3 diamond + 22 arrow + 26 text），`bounds=[-9,50,720,2180]` 有限、`canvasNonWhite=9880`、浏览器错误 `[]`。
     · 两轮后端日志：`Exception` 计数 0、`Resolved` 计数 0。
  -> 错误路径专项验证（把 8080 临时切成 `--llm.base-url=http://127.0.0.1:9/...` 死端口实例）：纯 SSE 探针 -> 0.21 秒内收到 `thinking` + `error` 两个事件且**流正常收尾**（修复前会悬挂）；真实浏览器 -> 失败后 8 秒内 `inputDisabled=false`、`stillLoading=false`，且能继续发第二条消息并再次拿到清晰错误；该实例日志 `Exception` 0、`HttpMessageNotWritable` 0。验证后已恢复 8080 为真实配置。
  -> ⚠️ 量级认知（本轮新得出，重要）：LLM 输出规模与耗时近似线性（约 40 token/秒，且推理模型先产出数千 reasoning token）。实测：5 元素 25s / 47 元素 50s / 41 元素 111s。**并发生成会显著互相拖慢** —— 首轮失败的 150s 与当时同时段另有 2 个请求在跑高度相关（同一账号吞吐共享），单独重跑同类需求仅 50s。故 150s 这道闸门既"对复杂图偏紧"、又会被并发放大。
  -> 遗留（未修，待拍板）：① 长流程会被 LLM 排成"竖长条"（c3 宽 729 × 高 2130），Excalidraw 适配后 zoom 降到 0.2，文字需手动放大才看得清 —— 是否给 `scrollToContent` 加"最小 zoom 下限"或让提示词约束布局纵横比；② `SpringBootTest` 仍缺失（应用配置只有本地 `application.yml`）；③ 并发请求无上限控制（每请求一个裸线程）。
  -> 门禁证据：后端 `clean test` -> `Tests run: 45, Failures: 0, Errors: 0` + `checkstyle:check` -> `0 Checkstyle violations`；前端 `tsc -b` / `eslint` 均 exit 0。
  -> 状态：代码与门禁完成，端到端已由 Agent 亲自验证通过；待用户在自己浏览器复验。
