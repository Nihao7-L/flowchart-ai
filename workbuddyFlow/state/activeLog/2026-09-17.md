# activeLog.md — 执行流水（外置记忆，只追加）

> 谁在什么时候做了什么、证据是什么。每条必有证据（命令 + 结果），否则等于没记。
> 两种读法：① 会话恢复 → 只读顶部「当前态」+ 尾部两三条首行，30 秒续上；② 人复盘 → 展开每条缩进详情。

## 当前态
阶段：**S1 进行中 ——"看得见"主干已通（真实出图 + 双通道 + 用户亲手验过），且 09-17 下半场把盘点出的缺口清掉大半，**仅剩 v2-36 布局实施与归档/提交**）**。M1 核心链路（v2-8~11，含补记 v2-32~v2-35 与 v2-37）+ M6a（v2-23 设计稿 / v2-24a 画布）均已落盘并端到端出图。09-17 完成：Bug B 自愈判据改**边界不变量**（旧「|坐标|≥1e6」漏掉 -3907 中段污染）、新增 `e2e_drag_stress` 多方位多距离拖拽回归、用户批准的垃圾清理、**A3 轮次语义修正（通过即发）**、**A1b 编辑模式几何保真（v2-37 `SceneDrift`）**、**A2 删除前端 layout 兜底**、**A4 frame 语义定案"方案 a"**、**v2-36 两条决策拍板（并补进第三种"新建/编辑"两模式硬约束）**，以及全量文档漂移收敛（architecture 6 处 / constraint 2 处 / coreFun 5 处 / Metadata 8 处 / 4 份计划，新建 v2-37 归档件）。门禁：后端 **102 测试**全绿 + checkstyle 0 违规 + 前端三绿 + 冒烟 10/10。| 等待：① 工作树（**85 项**改动）待用户 commit —— `git` 写操作由用户执行 ② v2-36 布局**实施**（决策已定，代码未动）③ S1 闭合时的归档动作（v2-23 / v2-24a 移 `state/completed/underway/`）| 下一步：实施 v2-36 `SceneLayout`（分"新建全量重排 / 编辑只给新增元素定位"两种模式）→ 真实 LLM 端到端复跑肉眼验（不重叠、箭头可见、标签不盖线）→ v2-24a/v2-23 归档 → S1 闭合进 S2

## 格式
首行摘要（给恢复现场扫一眼）+ 缩进详情（给复盘展开）。每条只装一个动作；补记加 `[补]` 前缀；**每次追加顺手更新顶部「当前态」三段（阶段 | 等待 | 下一步）**。
- MM-DD HH:mm 动作摘要（一行）
  → 细节 / 证据（命令 + 结果）
  → 状态：已提交 / 待审 / 等用户 commit

## 流水（只追加）

- 09-17 11:20~12:00 修 Bug B 判据（阈值 → 边界不变量）+ 前端标签归位 + 新增拖拽压力回归
  → 用户报"箭头消失 / 验证通过文字动不了"。根因：`healCorruptedArrows` 原用「坐标绝对值 ≥ 1e6」判污染，而真实污染是 **-3907 这种中段值**，被阈值漏掉 → 箭头被甩在画布外、其标签跟着跑到画布外（=用户看到的两个症状其实是同一个病）。
  → 修复：判据改**边界不变量** —— 两端端点到「期望包围盒交点」（`edgePointTowards`）的偏差之和 > `ARROW_HEAL_TOL=300` 即判污染（合法连线偏差 ≤~25px，污染 ≥上千 px，安全分离）；仅对被修过的箭头把标签挪回新中点。仅改 `frontend/src/lib/sceneAdapter.ts` + `Canvas.tsx` 注释。
  → **重大发现（纠正了一个 100% 假阳性的测试断言）**：绑定箭头标签的 `stored x/y` 平日就是过期的 —— 像素级实测（探针 `probe_label_render.py`）拖 n1 后箭头中点 (480,143)→(606,63)，**新中点处 682 个暗像素、stored 处 0**。⇒ Excalidraw 按箭头几何现算渲染。旧断言拿「stored 标签中心 vs 线中点」比必然误报，已改为「标签必须仍绑定在该箭头上」。
  → **重大发现（自动化必须清选中）**：拖动会让图形进入选中态，Excalidraw 的 `selected-shape-actions` 面板（x≈262~450，`pointer-events:all`）盖住画布、吞掉下一次 pointerdown → 表现"怎么拖都不动"。`keyboard.press("Escape")` 在 headless **不生效**，必须用 `updateScene({appState:{selectedElementIds:{}}})`。
  → 测试自身两处缺陷同修：每次拖动前 `fit` 会改 zoom（屏幕位移≠场景位移，曾出现"期望 300 实际 375"）→ 改 `dx × zoom`；图形重叠后中心可能被箭头抢命中 → 加 5 个候选抓取点，按下后核实 `selectedElementIds` 含目标。
  → 新增 `workbuddyFlow/tools/e2e/e2e_drag_stress.py`：14 个方向/距离拖动 + 12 次锯齿 + 拖到重叠再拖回 + 拖动途中改缩放 3 轮；逐帧 watcher（3473~3896 帧，最差脱靶 18px，越界 0 帧）。
  → 证据：浏览器 E2E **6/6 PASS**；后端 `clean test` 86 全绿 + `checkstyle` 0 违规；前端 `tsc -b`/`eslint --ext ts,tsx`/`vite build` 三绿。
  → 状态：代码已落盘，等用户 commit（`frontend/src/lib/sceneAdapter.ts`、`frontend/src/components/Canvas.tsx`、`workbuddyFlow/tools/e2e/e2e_drag_stress.py` 均在未跟踪目录里）

- 09-17 12:40~13:00 垃圾清理（用户："垃圾清理一下吧"）
  → 先只读扫描 + grep 确认无引用，再删。删除项（全部可再生、无一是源码）：仓库根 `blocker.png`（探针截图，**未被 ignore**、会污染 git status）、`tools/e2e/_shots/`（8 张回归截图）、`tools/e2e/probe_*.py`（7 个一次性探针）、`.playwright/diag/`（7 探针 + `pointer.log`）、`.playwright/shots/drag_stress_after.png`、`target/`、`frontend/dist/`。
  → 结果：`git status -uall` 里带 `.png/.log/probe_` 的条目 16 → **0**；`tools/e2e/` 只剩 6 个正式回归 + `harness.py`；E2E 清理后实测 `bash .playwright/run-e2e.sh e2e_arrow_unbind` **PASS**。
  → 顺带修正：`.playwright/run-e2e.sh` 的测试清单长期只列 5 个 → 补第 6 个 `e2e_drag_stress`（`bash -n` 通过）。
  → **保留（判定不是垃圾）**：`.playwright/{pkg,browsers}` 817M（自洽工具链：`PYTHONPATH` + `PLAYWRIGHT_BROWSERS_PATH` + managed python，不依赖 C 盘）；`frontend/tsconfig.*.tsbuildinfo`、`node_modules/.vite`；仓库根 `.workbuddy/`。
  → 状态：已完成

- 09-17 13:00~13:30 追查 C 盘 `%LOCALAPPDATA%\ms-playwright` 1.4G 的归属（用户选"先查谁在用，再定"）
  → **决定性证据**：`ms-playwright\.links\` 是 Playwright 自己留的注册账本（文件名 = hash，**内容 = 注册者的包目录**）。C 盘恰好两个注册者：`c5da9699…` → managed python venv 的 playwright **1.62.0**（要 chromium **1234**）；`b578815f…` → E 盘 npx 缓存的 playwright-core **1.60.0**（要 chromium **1223**，系统 python `D:/Python-Home` 也白嫖这一份，它只"用"不"装"）。
  → 对比 F 盘 `.playwright/browsers/.links`：只有**一个**注册者（项目自带 playwright 1.63 → chromium **1243**）。⇒ **F/C 两套零重叠、互不依赖**。
  → 结论：C 盘 1.4G **不是垃圾**（两个 chromium 修订都在被真实使用），未动。反之 F 盘 817M 与 C 盘 1223 功能重复，"零下载腾空间"的方向是把 `run-e2e.sh` 改用系统 python 1.60 —— 待用户拍板，本次未动。
  → 只读排查手法（可复用）：`where.exe <cli>` 定位全局安装；包内 `browsers.json` 的 `revision` 决定它要哪份 chromium；`<browsers>/.links/*` 的**文件内容**是判断"这套浏览器谁在用"最直接的证据。
  → 状态：已完成（结论：保留，未删任何字节）

- 09-17 16:40 S1 收尾盘点（只读，用户："S1 还有什么没有收尾的，还有什么问题"）
  → 结论：S1 主干已通但远未闭合，欠账分四类。**A 类（真缺口）**：① `ops`/`spec` 双协议未实现（`architecture.md` 36~38、`m1:53`、`m6a:31/37/43/44`、`m6b:32/38` 都写着，实现侧 `ChatEvent` 只有 thinking/validation/result/error，全仓搜 ops/spec 零命中，`result` 塞全量 JSON、前端 `App.tsx:59` 整体替换）；② 前端 `layout` 兜底不存在；③ 超时丢结果 + 跑满 3 轮白等；④ frame 语义未拍板；⑤ 并发无上限（归 M7）。**B 类**：`v2-32 / v2-33 / v2-34 / v2-35` 在代码里有编号，但 `plans/masterPlan`、`plans/underway`、`state/completed`、`state/activeLog` **全部零痕迹** → 永远归不了档。**C 类（文档/状态机漂移）**：今天没有 activeLog（本文件即为补记）、`state/active/` 空、`v2-23-design-spec.md` 已完成仍在 underway、`m6a-render.md` 状态行仍写"规划中"、`PATCH /api/model/positions` 在 `architecture.md`/`m1`/`m6b` 共 10 处残留（实现早已是 `PUT /api/model/canvas`，且同一 docs 下 `FlowAI架构与交互机制.md` 已用新名）、`coreFun.md` 功能表 1~5 项状态过期、`Metadata.md` 变更记录停在 09-15、`m1` 验收口径 3「记 tech-debt」失去载体（该 tracker 已于 09-11 被删）、`App.tsx:17,200` 注释残留旧编号 v2-24b（已纠错为 v2-31）。**D 类（环境/工程）**：`docker-compose.yml` 缺 base-url/model、`SpringBootTest` 缺失。
  → **关键澄清**：`ops` 不是"漏做的功能"，而是"**文档没更新的决定**" —— `PromptService:103` 明确要求 LLM 输出"**完整场景（全量 elements），不是差异片段**"。所以"S1 只走全量"这个决定早就做了，只是做在提示词里没人记账。用户 17:0x 确认口径：**ops 归 S3（v2-25 Diagram Tools），S1 只全量**。
  → **另一关键发现**：`PromptService:104~105` 已承诺"**没有要求改动的元素必须原样保留 id/type/几何（x/y/width/height）**"，但 `GraphValidator` 不查、`detectRewrite` 只查"整图被换掉"→ **几何漂移无人管**；而 v2-36 的"全量重排 layout"会**直接打破这条承诺** ⇒ v2-36 的布局必须区分两种模式：**新建 → 全量重排；编辑 → 保留未改动元素坐标、只给新元素定位**。
  → 状态：只读盘点，未改任何文件（清单已交用户待拍板）

- 09-17 17:00~17:10 A3 轮次语义修正：通过即发（用户已授权"现在就动 A3"）
  → 背景：用户截图那次"LLM 调用失败: LLM 调用超时: 整次调用超过 296 秒"的真因。旧语义是"每轮通过都先存档、**继续跑满 3 轮**、最后才发"，而第 2/3 轮用的 prompt 与第 1 轮**完全相同**（通过时不会构建修正 prompt，`buildFixPrompt` 只在校验失败时才调）——等于靠采样随机性重摇一次。两个后果：① 简单图也要 3 次 LLM 调用，用户等待时间直接 ×3；② 第 2/3 轮一旦撞上单轮上限 300 秒，**那份已经合格的存档被彻底丢掉**（296 秒正是 `planRound` 算出的"单轮上限 − 收尾余量"）。
  → 修复（`GenerationService.java`，4 处）：① 校验 0 issue → **立即** `saveModel` + 发 `result` + `complete()` + return（删掉原来的 `if (round < MAX_ROUNDS) continue;`）；② `lastGoodJson` 及其 3 处存档分支**整体删除** —— 通过即发之后它恒为 null（不可能在下一轮还是非空），留着就是**不可测的死代码**；③ 预算不足分支改为直接带原因报错（到得了那里说明前面每轮都没过，本就没有合格结果可发）；④ `MAX_ROUNDS` 注释重写为"1 次首生成 + 最多 2 次修正轮（仅在有 issue 时进入）"。
  → **重要说明（与原 v2-36 计划步骤 10 的偏差）**：计划写的是"catch 里补第三条兜底：有存档就发存档"。实现时发现 **首轮即发让这条兜底失去触发路径** —— 旧 D4「先存后发」补偿的正是"通过之后还空跑两轮"这个自己制造的风险；把这个风险去掉，补偿机制自然作废。按"不留死代码"处理，改为**删除**而非新增。用户若希望将来恢复多轮改进，需同时恢复它（届时它自有测试）。
  → 测试同步（`GenerationServiceTest.java`，14 例仍全绿）：`firstRoundSuccess` times(3)→**times(1)**；`retryOnValidationFailure` times(3)→**times(2)**；`flagsFullRewriteDuringEdit` times(3)→**times(2)**；`noRewriteCheckWithoutPreviousModel` times(3)→**times(1)**；原 `lastGoodJsonSentOnMultiRoundPass` → 改写为 `firstPassIsSentImmediately`（断言只调 1 次 + 发的是首轮结果"a"而非第二轮的"b"）；原 `savedResultUsedWhenLastRoundFails` → 改写为 `noLaterRoundAfterFirstPass`（断言 1 次调用 + `buildFixPrompt` 从未被调用 + 发 result）；`allRoundsFailNoSavedResultStillErrors` → 更名 `allRoundsFailStillErrors`（去掉已不存在的"无存档"措辞，断言不变：3 轮全败 → error）。
  → 证据：`mvn clean test` → **Tests run: 86, Failures: 0, Errors: 0, Skipped: 0** + `BUILD SUCCESS`；`checkstyle:check` → **You have 0 Checkstyle violations** + `BUILD SUCCESS`；前端回归 `tsc -b` / `eslint . --ext ts,tsx` / `vite build` → **三者 exit 0**（本步未改前端，做确认）。
  → 效果：简单图 LLM 调用 3 → **1** 次（等待时间约 ÷3）；"合格结果被超时白扔"这条路径**从构造上消失**（没有再需要兜底的后继轮次）。
  → 未做（需用户拍板后再说）：真实 LLM 端到端复跑（要起后端 + 消耗真实调用，按红线需先征得同意）。
  → 状态：代码与测试已落盘，**待用户 commit**（`src/main/java/.../service/GenerationService.java`、`src/test/java/.../service/GenerationServiceTest.java`）

- 09-17 17:10~17:5x S1 收尾四项拍板落地：A1b 几何保真（v2-37）+ A2 布局归属 + A4 frame 方案 a + v2-36 两条决策（用户原话："① A1b ② A2 ③ A4（a/b/c）④ v2-36 都按照你推荐的来，然后测试更新文档"）
  → **A1b · 编辑模式几何保真（v2-37）**：新增 `graph/SceneDrift`（`@Component`，只比对不改数据）。判据保守优先：两版都存在的可比元素 ≥ 3、其中漂移 ≥ 3 且占比 ≥ 50% 才判"整图重排"；**整体平移放行**（`isUniformTranslation` 用**中位数**而非平均值 —— LLM 顺手改一个元素不会带偏中位数）；两端都带 `id` 的箭头（几何由端点推导）与 `freedraw` 不参与；坏 JSON 交给 `GraphValidator`、`catch` 返回 null 不重复告警。接入点：`GenerationService` 编辑语境首轮（`editMode && !takeOver && round == 1`）的 `detectRewrite` 之后。**为什么必须先于 v2-36**：`PromptService:104~105` 已承诺"未改动元素原样保留 id/type/几何"，而 v2-36 的全量重排会**直接打破**它 —— 布局落地前，这条承诺必须先变成会拦人的校验（否则两者打架）。
  → **A2 · 布局归属**：删除 `m6a-render.md` 里"前端 `layout` 仅兜底"这条交付项；布局统一归后端 `graph/`（`architecture.md` 2.4 包表 + ADR-4），由 v2-36 交付；前端只消费坐标，不做任何布局。
  → **A4 · frame 语义定案"方案 a"**：`scene.schema.json` 的 frame 分支**不加** `children`（只补 `description`，写明 children 是渲染器实现细节、由前端适配层补、LLM 不要输出），缺口由前端**唯一**适配层 `frontend/src/lib/sceneAdapter.ts` 的 `normalize()` 补 `children: []`，并用 `frontend/tools/scene-smoke.mjs` 的 frame 用例锁住。被否的两条：加进契约（LLM 会乱填）/ 砍掉 frame 类型（白丢一种元素）。`GraphValidator` 相应补注释说明为何**不**校验 `children`。
  → **v2-36 两条决策拍板 + 计划修订**：① 不引 elkjs/dagre，手写分层布局（与 `GraphValidator` 弃 networknt 的手写风格一致，且避免新依赖所需的网络下载）；② 不改 `scene.schema.json` 的必填字段集（改它要同步 prompt / 校验器 / 前端适配层 / 契约测试，只为省几个 token 不划算）。**另补进一条硬约束（决策 3）**：布局必须分**新建（全量重排）/ 编辑（只给新增元素定位，未改动元素坐标一个都不动）**两种模式 —— 否则 v2-36 会与刚落的 v2-37 互相打架。同时把计划步骤 10 的旧文字（"补第三条兜底：有存档就发存档"）**标注作废**并写明原因（首轮即发后无触发路径，`lastGoodJson` 已成死代码），验收口径第 6 条同改；覆盖测试数据补"布局·新建 / 布局·编辑"两行。
  → **测试**：新增 `SceneDriftTest` **14 例**（identicalScenePasses / roundingTolerancePasses / singleMovePasses / labelOnlyChangePasses / fullRelayoutIsFlagged / uniformTranslationPasses / resizeIsFlagged / tooFewKeptElementsSkips / minorityDriftSkips / addedElementsDoNotCount / deletedElementsDoNotCount / boundArrowGeometryIsIgnored / freeLinePathChangeCounts / malformedInputsReturnNull）；`GenerationServiceTest` 新增 2 例（`flagsGeometryRelayoutDuringEdit` / `allowsUniformTranslationDuringEdit`）。
  → 证据：`mvn clean test` → **Tests run: 102, Failures: 0, Errors: 0, Skipped: 0** + `BUILD SUCCESS`（86 → 102）；`mvn checkstyle:check` → **You have 0 Checkstyle violations**；前端 `tsc -b` / `eslint . --ext ts,tsx` / `vite build` → **三者 exit 0**；`frontend/tools/scene-smoke.mjs` → **SMOKE PASSED: 10/10**。其中 frame 用例改为断言"适配层补丁让整批转换走通"——实测转换器**产物**字段是 `boundElements`/`frameId`、没有 `children`，`children` 只是**入参**要求（删补丁则 `TypeError: children.forEach` → fatal + dropped）。
  → **文档收敛（本轮）**：`docs/architecture.md` **6 处**（`PATCH /api/model/positions` → `PUT /api/model/canvas`（共 4 处）、"增量 ops / 全量 spec 双协议" → "S1 只走全量 `result`、ops 归 S3/v2-25"、"elkjs/dagre 布局" → 后端 `graph/` 手写分层布局、开发视图包表 graph 行"渲染兜底"改"布局是本包职责"）；`docs/constraint.md` **2 处**（elkjs 表述改为后端手写布局 + **新增两条技术约束**：frame 方案 a、"编辑模式几何保真"承诺及其对布局的反向约束）；`docs/coreFun.md` **5 处**（"IR 无坐标"→ 元素场景 + 后端布局补坐标；能力量级表"未接后端"→ 实测证据；功能清单第 1~5/9/10 行状态由"待做/部分"升为"已有/部分"并补 v2-32/34/37）；`docs/Metadata.md` **8 处**（版本 / 状态 / 最后更新 / 设计稿行 + **变更记录补 09-16、09-17 两行** + 当前阶段新增"当前阶段"行、进度查看补归档目录）；4 份计划（`m6a-render.md` 3 处、`v2-36-layout.md` **15 处**、`v2-23-design-spec.md` 1 处、`v2-24a-canvas-embed.md` 1 处）；新建归档件 `state/completed/underway/v2-37-edit-fidelity.md`。
  → **未做（明确留白）**：真实 LLM 端到端复跑、浏览器 E2E 复跑（本次未改前端，判定不受影响）；v2-36 布局代码（决策已定，实施留给下一步）。
  → 状态：代码 / 测试 / 文档均已落盘，**待用户 commit**（`git` 写操作由用户执行）
