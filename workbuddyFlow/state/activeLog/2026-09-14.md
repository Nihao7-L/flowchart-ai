# activeLog.md — 执行流水（外置记忆，只追加）

> 谁在什么时候做了什么、证据是什么。每条必有证据（命令 + 结果），否则等于没记。
> 两种读法：① 会话恢复 → 只读顶部「当前态」+ 尾部两三条首行，30 秒续上；② 人复盘 → 展开每条缩进详情。

## 当前态
阶段：**M1 已启动；路线图已重排为 S0~S6 执行序**。M0 全闭合（v2-1 ~ v2-7）；旧链路已清算删除，后端 API 现存仅 `GET /api/health`；测试 36 全绿、checkstyle 0 违规。执行序：S0 底座 ✅ → **S1 看得见（M1 + M6a）** → S2 更准（M2）→ S3 能改（M3 + M6b①）→ S4 会规划（M4 + M6b②）→ S5 不丢（M5）→ S6 可追踪（M7）；M6 已拆为 `m6a-render.md` / `m6b-interaction.md` 两文件，M5 内存版下沉至 M1 交付 | 等待：用户评审本轮改动 + commit + push（改动仍未提交）；另可在本地跑一次 `run-verify.ps1` 完整端到端补齐验收口径 1 | 下一步：**S1 的 v2-8 出图 JSON Schema 契约固化**

## 格式
首行摘要（给恢复现场扫一眼）+ 缩进详情（给复盘展开）。每条只装一个动作；补记加 `[补]` 前缀；无 vX-N 编号（改用时间戳，因现无统一可引用编号）；**每次追加顺手更新顶部「当前态」三段（阶段 | 等待 | 下一步）**。
- MM-DD HH:mm 动作摘要（一行）
  → 细节 / 证据（命令 + 结果）
  → 状态：已提交 / 待审 / 等用户 commit

## 流水（只追加）

- [补] 09-14 08:30 状态机文档统一到「completed 按来源分两层」新约定 + `m0-harness.md` 归档
  → 15 处规则性文档改动（AGENTS 4 / masterPlan-README 2 / underway-README 1 / underway-TEMP 2 / masterPlan-TEMP 1 / state-active-README 4 / completed-README 整篇重写），Python 原子重写 + 每处 count==1 断言全中；`plans/masterPlan/m0-harness.md` → `state/completed/masterPlan/m0-harness.md`；顺手修正 `state/active/README.md` 三处失效相对路径
  → 状态：等用户 commit

- 09-14 09:22 M1 前置代码清算决策拍板
  → 用户答三问：① 后端**不保留**导出下载能力 ② `static/` 旧 UI **立即删** ③ **要**一份清算清单文件；其余按 AI 判断清理
  → 勘验先决：`git status` 0 未提交项、HEAD `4d724f4` 与远端一致 → 删除可回滚；grep 确认 DiagramService / GenerateRequest / DownloadRequest 无旁路引用
  → 状态：待审

- 09-14 09:23 删除旧链路代码与静态资源
  → `rm`：`resources/static/`（index.html 150 + app.js 244 + style.css 342 = 736 行）、`DiagramService.java`（258）、`DiagramServiceTest.java`（254）、`GenerateRequest.java`（17）、`DownloadRequest.java`（14）；`pom.xml` 摘掉 `plantuml` 依赖块；`git status` 7 项 `D` 可见
  → 状态：待审

- 09-14 09:24 `DiagramController` 收敛为仅 `GET /api/health`
  → 删 `/api/generate` + `/api/download` 及其实参 import 与三个 `@Autowired`；`@Tag` 描述注明 `/api/chat` 由 M1 补入；`DiagramControllerTest` 重写 15 → 3（health 契约 + 两条「旧端点不再被映射 404」防回归）
  → 状态：待审

- 09-14 09:25 `ModelContractTest` 移除旧请求 record 用例（9 → 6）
  → 删 generateRequestAccessors / generateRequestEquality / downloadRequestAccessors 三例 + `assertNotEquals` import；类注释注明 record 随清算删除、M1 引入新请求体后再补
  → 状态：待审

- 09-14 09:25 清掉 3 处「面试」框架化注释，并修掉 `PromptService` 的真实并发缺陷
  → `LlmProvider` 注释「面试考点：依赖倒转原则」→「设计约束：依赖倒置（见 ADR-3）」；`PromptService` 模板缓存 `HashMap` → `ConcurrentHashMap`（懒加载 + Spring MVC 多线程，HashMap 并发 put 可致链表成环、后续 get 死循环），原注释以「个人项目、请求量低」合理化该缺陷，已改写为说明真实原因；`grep -rn "面试" src/` 清零
  → 状态：待审

- 09-14 09:26 门禁验证：`mvn clean test` + `mvn checkstyle:check` 全绿
  → `Tests run: 36, Failures: 0, Errors: 0, Skipped: 0` → **BUILD SUCCESS**（26.7s，java 直启 Maven）；`You have 0 Checkstyle violations`（5.9s）；测试日志出现 `No mapping for POST /api/generate` / `No mapping for POST /api/download`，证明 404 防回归断言真生效（非假绿）
  → 状态：待审

- 09-14 09:29 落清算清单 + 给 M1 计划补「处置旧代码」步骤
  → 新增 `plans/underway/m1-pre-cleanup.md`：目标/边界/分层边界/前置/**处置清单（A 删 / B 顺带修的缺陷 / C 保留 / D 不动）**/测试连带影响/验收与实测/遗留与后续，并写明回滚点 HEAD `4d724f4`；`plans/masterPlan/m1-generation.md` 状态 规划中 → **进行中**、边界补「允许删除」、前置补清算完成、步骤加第 0 步、状态跟踪加 `[x]` 前置
  → 原 M1 计划的边界/步骤里**完全没有「处置旧代码」一环**（此前勘验发现），本次补上，避免新旧并存
  → 状态：待审

- 09-14 09:31 架构与 README 的旧链路表述同步更正（14 处）
  → `architecture.md` 2 处：2.4 包表 `graph/` 去掉「SVG/Mermaid 导出」（与 ADR-4「渲染在前端」冲突）、风险段「代码仍停留在旧架构」改写为「旧链路已清算」；`README.md` 12 处：Status 段更新至 09-14、删「多格式导出」特性行与技术栈行、测试 23 → 36 并展开测试类表、`flowchart-frontend/` → `frontend/`、API 表标注 `GET /api/health` 为当前唯一已实现端点、图表示例标注为历史、`docs/` 路径更正为 `workbuddyFlow/`
  → Python 原子重写 + count==1 断言，14/14 命中；残留 grep 清零（`flowchart-frontend` 仅存于 Metadata 历史变更记录，属史实保留）
  → 状态：待审

- 09-14 10:50 路线图重排：引入 S0~S6 执行序（用户批准「开始这个方案」）
  → 新增 3 文件：`plans/masterPlan/roadmap.md`（阶段 × 模块映射 + 每阶段「你能亲手验什么」+ 编号规则 + 代价）、`m6a-render.md`、`m6b-interaction.md`；删除 `plans/masterPlan/m6-frontend.md`（拆分后）
  → 改动 8 文件 / 54 处（Python 原子重写 + 每处 count==1 断言全中）：`masterPlan/README.md` 加「编号≠顺序」声明、`AGENTS.md` 索引加 roadmap 一行、`m1-generation.md` 11 处（阶段改 S1 / 边界允许新增 `session/` 内存版 / 分层边界 / 步骤 5 / 验收 5 / 覆盖数据多轮 / 状态跟踪）、`m5-session.md` 9 处（目标收窄为「Redis 实现 + 探活降级」/ 前置改 M1 交付内存版 / v2-21 步骤收窄 / 验收加降级透明）、`m2/m3/m4` 各补所属阶段行与前置标注、`m7-observability.md` 8 处（覆盖任务 v2-27~30，v2-30 归属落定）、`coreFun.md` 功能表加「所属阶段」列并更正 v2-3 状态漂移（待做→已有）、`architecture.md` 包表阶段改 S + ADR-2 补落地顺序 + 下一步改执行序、`Metadata.md` 6 处 + 变更记录 1 行
  → 残留核对：`m6-frontend` 仅存于归档件（`state/activeLog/2026-09-11|12.md`、`state/completed/underway/v2-6-*`）与历史变更记录 —— 属史实，按「归档件不可回改」原则保留；`（阶段N）` 旧标注清零
  → 状态：等用户 commit
